import React from 'react';
import styles from './styles.less';

import {
    ContentWrapper,
    EntityCard,
} from '@/components';
import {
    CardBox,
    Controls,
} from '@/parts';

const Categories = () => {
    return (
        <ContentWrapper>
            <CardBox>

            </CardBox>
            <Controls />
        </ContentWrapper>
    );
};

export default Categories;
